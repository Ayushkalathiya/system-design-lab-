1. image and container
    images : blue print
    container : running images 

2. docker compose : build and manage multiple containser in dev ( for prod kuberntees )

3. we have policy of restert and all for docker

4. volumes : is use for store data even if redis or that servevr failed so that we can retrive old data from this

5. we have retry policy for checking redis health

6. Docker has internal DNS - service names resolve to IPs automatically 

7. redis speaks on Redis Serialization Protocol (RESP) 